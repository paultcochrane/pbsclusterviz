 - implement the configuration file format that Holger recently used for the
   node list
 - can we get this to work with HLRN?
 - implement a setup.py so that one can install the package
 - write a program to automatically generate a nodes file
